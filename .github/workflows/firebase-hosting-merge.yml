# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
  # --- NEW: Add this line to enable the "Run workflow" button ---
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
        env:
          VITE_FIREBE_API_KEY: ${{ secrets.VITE_FIREBE_API_KEY }}
          VITE_FIREBE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBE_AUTH_DOMAIN }}
          VITE_FIREBE_PROJECT_ID: ${{ secrets.VITE_FIREBE_PROJECT_ID }}
          VITE_FIREBE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBE_STORAGE_BUCKET }}
          VITE_FIREBE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBE_MESSAGING_SENDER_ID }}
          VITE_FIREBE_APP_ID: ${{ secrets.VITE_FIREBE_APP_ID }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_REPFORGE_APP_BD6F5 }}'
          channelId: live
          projectId: repforge-app-bd6f5